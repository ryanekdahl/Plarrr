% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_plastics.R
\name{plot_plastics}
\alias{plot_plastics}
\title{Top plastic types for a country}
\usage{
plot_plastics(data, country, n = 3)
}
\arguments{
\item{data}{A data frame from \code{\link[=load_data]{load_data()}} or with the same structure.
Must contain \code{country}, \code{parent_company}, \code{grand_total}, and the plastic
columns \code{pet}, \code{hdpe}, \code{ldpe}, \code{pp}, \code{ps}, \code{pvc}, \code{o}.}

\item{country}{Character. The country to plot. Matched against the
\code{country} column on \code{parent_company == "Grand Total"} rows.}

\item{n}{Integer. Number of top plastic types to show. Defaults to \code{3}.}
}
\value{
A \code{ggplot} object.
}
\description{
Horizontal bar chart of a country's \code{n} most-collected plastic types, with
each bar labelled by the share (percent) of that country's total plastic
collected. If a country has multiple rows (e.g. multiple years), counts are
summed before ranking.
}
\examples{
\dontrun{
df <- load_data()
plot_plastics(df, country = "Switzerland")
}
}
