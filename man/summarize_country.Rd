% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_function.R
\name{summarize_country}
\alias{summarize_country}
\title{Summarize plastic composition for a single country}
\usage{
summarize_country(data, country_name)
}
\arguments{
\item{data}{A data frame from \code{\link[=load_data]{load_data()}} or with the same structure.
Must contain the columns \code{country}, \code{parent_company}, \code{grand_total},
\code{pet}, \code{hdpe}, \code{ldpe}, \code{pp}, \code{ps}, \code{pvc}, and \code{o}.}

\item{country_name}{Character. Name of the country to summarize. Must match
a value present in \code{data$country}.}
}
\value{
A one-row tibble with columns:
\describe{
\item{country}{The country name.}
\item{avg_pet, avg_hdpe, avg_ldpe, avg_pp, avg_ps, avg_pvc, avg_o}{
Average proportion of each plastic type across the country's records.}
\item{leading_plastic}{The plastic type with the highest average
proportion, formatted in upper case ("PET", "HDPE", …) or "Other"
for the \code{o} category.}
}
}
\description{
Computes the average proportion of each plastic type collected in a given
country and identifies the most common plastic type. Proportions
are calculated as plastic-type count divided by \code{grand_total}, averaged
across all years available for that country.
}
\details{
Only rows where \code{parent_company == "Grand Total"} and \code{grand_total > 0} are
used, matching the per-country totals reported in the TidyTuesday plastics
dataset.
}
\examples{
\dontrun{
df <- load_data()
summarize_country(df, "India")
}
}
