% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_country.R
\name{filter_country}
\alias{filter_country}
\title{Filter plastics data to one country's Grand Total rows}
\usage{
filter_country(data, country, positive_only = FALSE)
}
\arguments{
\item{data}{A data frame from \code{\link[=load_data]{load_data()}} or with the same structure.
Must contain \code{country}, \code{parent_company}, and \code{grand_total} columns.}

\item{country}{Character. A single country name to keep. Matched against the
\code{country} column.}

\item{positive_only}{Logical. If \code{TRUE}, also drop rows where
\code{grand_total == 0}. Defaults to \code{FALSE}.}
}
\value{
A data frame with the same columns as \code{data}, containing only the
Grand Total rows for \code{country} (and, if \code{positive_only = TRUE}, only those
with \code{grand_total > 0}).
}
\description{
Helper used by \code{\link[=plot_plastics]{plot_plastics()}} and \code{\link[=summarize_country]{summarize_country()}} to subset the
TidyTuesday plastics data to the per-country aggregate rows
(\code{parent_company == "Grand Total"}) for a single country. Optionally drops
rows whose \code{grand_total} is zero, which is useful when downstream code
divides by \code{grand_total}.
}
\examples{
\dontrun{
df <- load_data()
filter_country(df, "India")
filter_country(df, "India", positive_only = TRUE)
}
}
