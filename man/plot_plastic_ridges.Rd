% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_plastic_ridges.R
\name{plot_plastic_ridges}
\alias{plot_plastic_ridges}
\title{Weighted density ridges of an environmental indicator by plastic type}
\usage{
plot_plastic_ridges(
  data,
  plastics = c("pet", "hdpe", "ldpe"),
  env_var = "threatened_species",
  log_x = TRUE
)
}
\arguments{
\item{data}{A data frame from \code{\link[=load_data]{load_data()}} or with the same structure.
Must contain \code{country}, \code{parent_company}, \code{grand_total}, the plastic
columns named in \code{plastics}, and the column named in \code{env_var}.}

\item{plastics}{Character vector. Plastic-type columns to plot, drawn from
\code{c("pet", "hdpe", "ldpe", "pp", "ps", "pvc")}. Order here controls the
factor order on the y axis. Defaults to \code{c("pet", "hdpe", "ldpe")}.}

\item{env_var}{Character. Name of the environmental-indicator column to put
on the x axis. Defaults to \code{"threatened_species"}.}

\item{log_x}{Logical. If \code{TRUE} (default), the x axis is on a log10 scale.}
}
\value{
A \code{ggplot} object.
}
\description{
For each selected plastic type, draws a weighted density ridge of an
environmental indicator (e.g. \code{threatened_species}) across countries. Each
country contributes a weight equal to the quantity of that plastic type it
collected, so high-volume collections drive the density.
}
\details{
Only rows where \code{parent_company == "Grand Total"} and \code{grand_total > 0} are
used, mirroring \code{\link[=summarize_country]{summarize_country()}} and the TidyTuesday plastics data.
Rows missing the chosen \code{env_var} are dropped before plotting.
}
\examples{
\dontrun{
df <- load_data()
plot_plastic_ridges(df, plastics = c("pet", "hdpe", "ldpe"))
}
}
