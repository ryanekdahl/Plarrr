% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_category_metrics.R
\name{summary_category_metrics}
\alias{summary_category_metrics}
\title{Summarize plastic metrics by environmental-variable tier}
\usage{
summary_category_metrics(
  data,
  env_var = c("threatened_species", "forested_area")
)
}
\arguments{
\item{data}{A data frame from \code{\link[=load_data]{load_data()}} or with the same structure.
Must contain \code{country}, \code{parent_company}, \code{grand_total}, the plastic
columns (\code{pet}, \code{hdpe}, \code{ldpe}, \code{pp}, \code{ps}, \code{pvc}, \code{o}), and the chosen
\code{env_var}.}

\item{env_var}{Character. Environmental indicator to tier by. One of
\code{"threatened_species"} or \code{"forested_area"}. Defaults to
\code{"threatened_species"}.}
}
\value{
A tibble with one row per tier and columns:
\describe{
\item{tier}{Tier label (factor): \code{"Low"}, \code{"Medium"}, or \code{"High"}.}
\item{env_min, env_max}{Range of \code{env_var} covered by the tier.}
\item{n_countries}{Number of distinct countries in the tier.}
\item{avg_grand_total}{Mean total plastic collected per row in the tier.}
\item{avg_pet, avg_hdpe, avg_ldpe, avg_pp, avg_ps, avg_pvc, avg_o}{
Mean share (count / \code{grand_total}) of each plastic type in the tier.}
\item{leading_plastic}{Plastic type with the highest average share in
the tier (\code{"PET"}, \code{"HDPE"}, ..., or \code{"Other"}).}
}
}
\description{
Bins each country-year \verb{Grand Total} row by an environmental indicator
(\code{threatened_species} or \code{forested_area}) into three quantile tiers
(\code{"Low"}, \code{"Medium"}, \code{"High"}), then summarizes plastic-collection
metrics within each tier. Useful for comparing plastic composition
across countries that share an environmental profile (e.g. high vs. low
forest coverage).
}
\details{
Only rows where \code{parent_company == "Grand Total"}, \code{grand_total > 0}, and
\code{env_var} is non-missing are used.
}
\examples{
\dontrun{
df <- load_data()
summary_category_metrics(df, env_var = "threatened_species")
summary_category_metrics(df, env_var = "forested_area")
}
}
